version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    privileged: true
    pid: host
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
